{
  "name": "abdelrahman-negm-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-first portfolio for Abdelrahman Negm — Mobile Engineer (Flutter).",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i src/styles.css -o dist/assets/styles.css --minify",
    "build:html": "node build.mjs",
    "build": "npm run build:css && npm run build:html",
    "dev": "npm run build && (cd dist && python3 -m http.server 4321)",
    "serve": "cd dist && python3 -m http.server 4321"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "tailwindcss": "^4.1.11"
  }
}
